version: "0.3.0"
description: "SKAPP Library — outbound HTTP/HTTPS calls from device to external services. Endpoint types: generic, IFTTT Maker, webhook_post. Methods: POST/GET/PUT/DELETE. Auth: NONE / Bearer / Basic / custom-header. NVS-persisted endpoint table, async worker, TLS via cert bundle, JSON escape."
url: "https://github.com/smartkraft/skapp-library"
license: "AGPL-3.0-or-later"
tags:
  - smartkraft
  - skapp
  - api
  - http
  - ifttt
dependencies:
  idf:
    version: ">=5.0"
